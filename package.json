{
  "name": "credia",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run start:dev -w @credia/api"
  },
  "engines": {
    "node": ">=20"
  }
}
